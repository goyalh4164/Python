# package is a collection of modules
# modules is a collection of functions
# with a package we need a init file
import shoping.shoping
import utility

print(utility) #it will cache the file for faster processing
print(utility.divide(2,3))
print(utility.multiply(2,3))
print(shoping.shoping.buy('apple'))